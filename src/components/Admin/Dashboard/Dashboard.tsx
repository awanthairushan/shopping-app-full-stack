import React, {FC} from "react";
import { Col } from "react-bootstrap";

const Dashboard: FC = () => {
    return (
        <Col lg={10}>
            Dashboard
        </Col>
    );
}

export default Dashboard;
