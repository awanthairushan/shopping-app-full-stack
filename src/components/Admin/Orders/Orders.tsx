import React, {FC} from "react";
import {Col} from "react-bootstrap";

const Orders: FC = () => {
    return (
        <Col lg={10}>
            Orders
        </Col>
    );
}

export default Orders;
