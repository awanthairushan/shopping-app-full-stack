import React, {FC} from "react";
import {Col} from "react-bootstrap";

const Customers: FC = () => {
    return (
        <Col lg={10}>
            Customers
        </Col>
    );
}

export default Customers;
