import React, {FC} from "react";
import {Col} from "react-bootstrap";

const Products: FC = () => {
    return (
        <Col lg={10}>
            Products
        </Col>
    );
}

export default Products;
