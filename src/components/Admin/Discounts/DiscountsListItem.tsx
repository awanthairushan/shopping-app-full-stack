import React, {FC} from "react";
import {Col} from "react-bootstrap";

const DiscountsListItem: FC = () => {
    return (
        <Col lg={10}>
            DiscountsListItem
        </Col>
    );
}

export default DiscountsListItem;
