export interface IProduct {
    name: string,
    id: string,
    price: string,
    discountedPrice: string,
    quantity: number,
    category: string,
    image: string,
    description?: string
}
