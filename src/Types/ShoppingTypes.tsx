export interface IProduct {
  name: string;
  price: string;
  oldPrice: string | null;
  img: string;
}

export interface ICart {
  name: string;
  price: string;
  quantity: string;
  img: string;
}
